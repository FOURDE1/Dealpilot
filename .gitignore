# Dependencies
node_modules/

# Build outputs
dist/
build/
.turbo/
coverage/

# Environment & secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
secrets/
credentials*

# Personal Claude Code overrides (machine-local)
.claude/settings.local.json

# OS / editor noise
.DS_Store
Thumbs.db
*.log
.gstack/
cdk.out/

# TypeScript incremental build state
*.tsbuildinfo

# F-13c local document storage driver (dev/CI)
apps/*/.storage/
