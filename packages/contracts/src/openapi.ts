/**
 * OpenAPI 3.1 document generation for the /api/v1 contract.
 *
 * Stub (A-03 scope). Real generation is wired in A-05 alongside the Fastify
 * mount — generated from `apiV1` with the ts-rest OpenAPI generator so the
 * served spec can never drift from the mounted contract. See docs/TASKS.md.
 */
export function generateOpenApiDocument(): never {
  throw new Error('OpenAPI generation not implemented yet — lands with A-05 (docs/TASKS.md)');
}
