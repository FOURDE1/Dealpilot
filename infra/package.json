{
  "name": "@dealpilot/infra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy --all"
  },
  "devDependencies": {
    "aws-cdk": "2.1132.0",
    "aws-cdk-lib": "2.261.0",
    "constructs": "10.7.1",
    "tsx": "4.23.1",
    "typescript": "catalog:"
  }
}
